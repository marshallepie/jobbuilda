{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/coordinator && pnpm build",
    "watchPatterns": [
      "apps/coordinator/**",
      "packages/**"
    ]
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c 'export PUPPETEER_EXECUTABLE_PATH=$(command -v chromium || command -v chromium-browser); echo \"[start] chromium=$PUPPETEER_EXECUTABLE_PATH\"; exec node dist/index.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
